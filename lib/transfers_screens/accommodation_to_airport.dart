import 'package:flutter/material.dart';

class AccommodationToAirportScreen extends StatelessWidget {
  const AccommodationToAirportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Add Accommodation To Airport Transfer'),), body: const Placeholder());
  }
}
