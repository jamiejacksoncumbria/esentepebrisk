import 'package:flutter/material.dart';

class AirportToAccommodationScreen extends StatelessWidget {
  const AirportToAccommodationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Add Airport To Accommodation Transfer'),),body: const Placeholder());
  }
}
